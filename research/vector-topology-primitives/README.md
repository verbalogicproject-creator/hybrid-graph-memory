# Vector Topology Primitives

The active, evidence-governed research bundle is `canonical/`.

Repository copies under `legacy/` preserve historical exploration for
auditability. In the standalone paper directory, the pre-existing `topology*.md`
files and `2/` directory have the same legacy standing. They are not benchmark
results, proofs, or current capability statements and are excluded from this
repository's default self-indexing configuration.

Current result: **hybrid retrieval superiority is not demonstrated.** The project
currently establishes an engineering artifact and a falsifiable evaluation
protocol—not a new mathematical theorem, manifold construction, or physics model.

Start with:

- `canonical/CLAIMS.md`
- `canonical/PROTOCOL.md`
- `canonical/paper.md`
- `canonical/evaluate.ts`
